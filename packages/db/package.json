{
  "dependencies": {
    "@octokit/webhooks": "catalog:",
    "@octokit/webhooks-types": "catalog:",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@aws-sdk/client-rds-data": "catalog:",
    "@aws-sdk/rds-signer": "catalog:",
    "@types/pg": "catalog:",
    "drizzle-kit": "catalog:",
    "ts-node": "catalog:"
  },
  "name": "@gitgazer/db",
  "peerDependencies": {
    "drizzle-orm": "catalog:"
  },
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "migrate": "node --env-file=../../.env -r ts-node/register scripts/run-drizzle-migrations.ts",
    "pretty": "prettier --ignore-path ../../.prettierignore --write .",
    "pretty:check": "prettier --ignore-path ../../.prettierignore --check .",
    "typecheck": "tsc --noEmit -p tsconfig.json --rootDir ../.. --ignoreDeprecations 6.0"
  }
}
