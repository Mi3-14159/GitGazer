{
  "description": "Backfill GitHub Actions workflow data into GitGazer via API",
  "devDependencies": {
    "@octokit/webhooks-types": "^7.6.1",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0"
  },
  "name": "@gitgazer/import",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "backfill": "node --env-file=.env -r ts-node/register -r tsconfig-paths/register src/index.ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "pretty": "prettier --write ."
  },
  "version": "1.0.0"
}
